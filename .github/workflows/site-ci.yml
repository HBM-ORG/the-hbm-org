name: Site CI

on:
  push:
    branches:
      - dev
      - staging
      - main
    paths:
      - "apps/site/**"
      - "config/.nvmrc"
      - ".github/workflows/site-ci.yml"
  pull_request:
    branches:
      - dev
      - staging
      - main
    paths:
      - "apps/site/**"
      - "config/.nvmrc"
      - ".github/workflows/site-ci.yml"
  workflow_dispatch:

concurrency:
  group: site-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate-site:
    name: Validate Site
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: config/.nvmrc
          cache: npm
          cache-dependency-path: apps/site/package-lock.json

      - name: Install dependencies
        run: npm ci --prefix apps/site

      - name: Validate site workspace
        run: npm --prefix apps/site run validate
