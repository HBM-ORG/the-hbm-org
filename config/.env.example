# Deprecated compatibility pointer.
# Use app-owned env files instead:
# - apps/server/.env.example -> apps/server/.env
# - apps/client/.env.example -> apps/client/.env
#
# The repo root `.env` is still read by some scripts during transition,
# but new setup should be based on the per-app env files above.
